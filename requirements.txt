# --- Core RAG stack ---
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-google-genai>=2.0.0    # Gemini (무료 티어) — 기본 생성 LLM
langchain-openai>=0.2.0          # (선택) OpenAI 대안
langchain-chroma>=0.1.4
chromadb>=0.5.0
rank-bm25>=0.2.2                 # 하이브리드 검색의 어휘(BM25) 축

# --- 로컬 무료 임베딩(기본) ---
langchain-huggingface>=0.1.0
sentence-transformers>=3.0.0

# --- API server ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Config / utils ---
pydantic-settings>=2.4.0
python-dotenv>=1.0.0
tiktoken>=0.7.0

# --- (선택) 로컬 무료 임베딩을 쓸 경우에만 설치 ---
# pip install "langchain-huggingface>=0.1.0" "sentence-transformers>=3.0.0"
#   → .env 에서 EMBEDDING_PROVIDER=hf 로 전환하면 OpenAI 키 없이 임베딩 가능(torch 다운로드 필요)
